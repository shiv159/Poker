# Redis operations

Use managed Redis with TLS, ACL authentication, AOF `appendfsync everysec`, periodic RDB snapshots, durable backup copies, and eviction disabled. `redis.conf.example` contains the local prototype durability baseline.

Monitor memory, connections, command latency, rejected connections, AOF size, and persistence failures. Never commit connection URLs or credentials.
